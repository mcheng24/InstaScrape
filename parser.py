#!/usr/bin/python3

from bs4 import BeautifulSoup
import webpage.py

class instaparse:
    __init__(self, profile):
        self.profile = profile
        if self.profile.links == []
            generate_links(self.profile.link)
        if self.profile.words == []
            make_wordlist(self.profile.links)
        if self.profile.dic = []
            make_dictionary(self.profile.words)
def generate_links(self, link):
def make_wordlist(self, links):
def make_dictionary(self, words):
